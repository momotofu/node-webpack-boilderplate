doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewPort" content="width=device-width, initial-scale=1")
    title My app
  body
    div#root
    script(src=`${mainBundlePath + bundleName}`)
